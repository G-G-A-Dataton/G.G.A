model:
  model_name: "cross-encoder/ms-marco-MiniLM-L-6-v2"
  max_length: 256
  batch_size: 32
  device: "cpu"

reranking:
  top_k_candidates: 100
  first_stage_weight: 0.3
  reranker_weight: 0.7
  loss_type: "listnet"
